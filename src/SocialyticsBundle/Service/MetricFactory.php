<?php

namespace SocialyticsBundle\Service;

/**
 * MetricFactory creates particular set of Metric implementation (eg. TwitterFollowers,..)
 * along with corresponding behaviors (ProviderRepository, Formatter).
 *
 * @author djuro
 */
class MetricFactory
{
   
    public function create($metricName, $resultType, $compare)
    {
        
    }
}
